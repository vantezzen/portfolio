# Vercel AI Gateway key for BenNet (local development only; on Vercel the
# deployment authenticates via OIDC automatically). Create one at
# https://vercel.com/dashboard -> AI Gateway -> API keys, then copy this
# file to .env.local.
AI_GATEWAY_API_KEY=
